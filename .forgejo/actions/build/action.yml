inputs:
  version:
    description: "version to use"
    required: true
    type: string
runs:
  using: "composite"
  steps:
    - name: Build, Checkstyle, Test
      shell: bash
      run: |
        make clean dependencies build-all checkstyle test
